<div class="menu">
    <nav>
        <p><a href="/manifesto">manifesto</a></p>
        <p><a href="/logout">logout</a></p>
    </nav>
</div>
<header>
    <h4>CALLING FOR A GREATER TRANSPARENCY, SUSTAINIBILITY AND ETHICS IN THE FASHION INDUSTRY</h4>
</header>
<div class="wrapper">
    <form method="POST">
        <h1>because you believe in making fashion fair</h1>
        <h2>sign our petition</h2>
        <input type="hidden" id="signature" name="signature" value="">
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
        <canvas id="canvas" class="draw signCanvas" width="550" height="190"></canvas>
        {{#if error}}
            <div class="error">something went wrong, try again</div>
        {{/if}}
        <button id="submit">submit</button>
    </form>
</div>
<script src="/canvas.js" charset="utf-8"></script>

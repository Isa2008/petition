<div class="menu">
    <nav>
        <p><a href="/manifesto">manifesto</a></p>
        <p><a href="/logout">logout</a></p>
    </nav>
</div>
<header>
    <h4>CALLING FOR A GREATER TRANSPARENCY, SUSTAINIBILITY AND ETHICS IN THE FASHION INDUSTRY</h4>
</header>
<div class="wrapper">
    <form method="POST">
        <h1>because there is no planet b</h1>
        <h2>change your profile</h2>
        <input type="text" class="inputfields" name ="firstname" placeholder="first name" value="{{firstname}}">
        <p class="profile">First Name</p>
        <input type="text" class="inputfields" name ="lastname" placeholder="last name" value="{{lastname}}">
        <p class="profile">Last Name</p>
        <input type="email" class="inputfields" name ="email" placeholder="email" value="{{email}}">
        <p class="profile">Email</p>
        <input type="password" class="inputfields" name="password" autocomplete="off" placeholder="password" value="{{password}}">
        <p class="profile">Password</p>
        <input type="number" class="inputfields" name ="age" placeholder="age" value="{{age}}">
        <p class="profile">Age</p>
        <input type="text" class="inputfields" name="city" placeholder="city" value="{{city}}">
        <p class="profile">City</p>
        <input type="text" class="inputfields" name="homepage" placeholder="homepage" value="{{url}}">
        <p class="profile">Homepage</p>
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
        {{#if error}}
            <div class="error">Something went wrong, try again</div>
        {{/if}}
        <button id="submit">update</button>
        <p class="linkto"><a href="/thanks">go to my signature</a></p>
    </form>
</div>

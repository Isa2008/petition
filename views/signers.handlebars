<div class="menu">
    <nav>
        <p><a href="/manifesto">manifesto</a></p>
        <p><a href="/logout">logout</a></p>
    </nav>
</div>
<header>
    <h4>CALLING FOR A GREATER TRANSPARENCY, SUSTAINIBILITY AND ETHICS IN THE FASHION INDUSTRY</h4>
</header>
<div class="wrapper">
    <form method="POST">
        <h1>thank you for signing❣️</h1>
        <h2>because we believe in making fashion fair</h2>
            {{#signers}}
                <p class="signers">
                    {{#if url}}
                <a href="{{url}}" target="_blank">{{firstname}} {{lastname}}</a>
                    {{else}}
                        {{firstname}} {{lastname}}
                    {{/if}}
                    {{#if age}}
                        {{age}}
                    {{/if}}
                    {{#if city}}
                        <a href="/signers/{{city}}">{{city}}</a>
                    {{/if}}
                </p>
            {{/signers}}
</div>
